"""Plotting helpers — pure logic only.

Submodules:
  - grids: pure computation for Ia/Gm/Rp/mu/Pa grids
  - grouping: CurveData builders and clustering helpers

Qt-dependent rendering (PlotRenderer, overlays) lives in ``app/plotting/``.
"""
